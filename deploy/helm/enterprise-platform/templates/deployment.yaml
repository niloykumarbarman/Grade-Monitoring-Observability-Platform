apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "enterprise-platform.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "enterprise-platform.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "enterprise-platform.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
