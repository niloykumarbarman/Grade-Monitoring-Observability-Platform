replicaCount: 1
image:
  pullPolicy: Always
ingress:
  enabled: true
  hosts:
    - host: dev.platform.local
      paths:
        - path: /
          pathType: Prefix
resources:
  limits:
    cpu: 250m
    memory: 256Mi
